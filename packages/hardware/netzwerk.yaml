speedtestdotnet:

device_tracker:
  - platform: nmap_tracker
    scan_options: " --privileged -sn "
    hosts:
      - !secret nmap_hosts                
    exclude:
      - !secret synology_host     
      