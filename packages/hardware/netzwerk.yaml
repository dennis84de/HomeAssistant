speedtestdotnet:

device_tracker:
  - platform: nmap_tracker
    scan_options: " --privileged -sn "
    hosts:
      - !secret nmap_hosts                
    exclude:
      - !secret synology_host     
      - !secret computer_host
      - !secret computerarbeit_host

  - platform: ping
    hosts:
      computer: !secret computer_host
      computerarbeit: !secret computerarbeit_host